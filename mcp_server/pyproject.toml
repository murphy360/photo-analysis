[project]
name = "photo-analysis-mcp"
version = "0.1.0"
description = "MCP adapter over the photo-analysis REST API"
requires-python = ">=3.12"
dependencies = [
    "mcp>=2.0",
    "httpx>=0.27",
]

[tool.uv]
package = false
